# GenericAgent 图解教程 · Visual Guide

> 一个面向 [GenericAgent](https://github.com/lsdefine/GenericAgent) 项目的双语图解教程。
> A bilingual illustrated guide to the GenericAgent project.

> ⚠️ **骨架 / Skeleton** — 本 README 为占位骨架，将在后续任务中补全。
> This README is a placeholder skeleton and will be expanded in a later task.

本教程为**双语**（中文 / English），可通过顶栏的语言切换按钮切换语言。
This guide is **bilingual** (中文 / English); use the language toggle in the top bar to switch.

## 🚀 如何阅读 / How to read

直接在浏览器中打开 `index.html` 即可（支持 `file://` 协议）。
Open `index.html` in a browser (works via the `file://` protocol).

可选地，启动一个本地服务器：
Optionally, start a local server:

```bash
python -m http.server 8000
```

## 🛠️ 重新生成 / Rebuild

教程由 `src/` 下无第三方依赖的 Python 3 脚本生成。
The guide is generated by dependency-free Python 3 scripts in `src/`.

```bash
cd src && python build.py
```

## 📁 项目结构 / Structure

```
generic-agent-visual-guide/
├── index.html              (generated later)
├── lessons/                (generated later)
├── src/                    no-dependency Python generators (filled in later tasks)
├── tests/                  Python unittest (filled in later tasks)
├── README.md
├── LICENSE
└── .gitignore
```
